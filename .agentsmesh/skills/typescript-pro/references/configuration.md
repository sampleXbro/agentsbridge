# Configuration

- Keep `strict: true`, `noUncheckedIndexedAccess`, and `exactOptionalPropertyTypes`.
- Use project references for larger repos to improve incremental builds.
- Emit declarations for libraries and shared packages.
- Prefer type-only imports where possible for cleaner output.
