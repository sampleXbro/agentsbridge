# Utility Types

- Use built-in utility types (`Pick`, `Omit`, `Partial`, `Required`, `Record`) first.
- Prefer composable aliases for domain-focused utility types.
- Avoid over-abstracting utility types that hide intent.
- Validate utility output with `satisfies` where possible.
