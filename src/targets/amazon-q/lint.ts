/**
 * Amazon Q Developer target-specific lint hooks.
 *
 * Amazon Q Developer does not support hooks natively — hooks are part of CLI
 * agent JSON configuration, not a canonical AgentsMesh feature file.
 * No hook-specific lint needed; placeholder for future extensions.
 */
