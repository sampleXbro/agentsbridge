# Lessons Learned

- **Generated Markdown root mirrors must be in the output-source map, not just decoration paths**: `.cursor/AGENTS.md` still leaked `.agentsmesh/skills/post-feature-qa/` after the shared link-rebaser consolidation because the source map only associated Cursor's root rule with `AGENTS.md`, not the additional `.cursor/AGENTS.md` mirror. Root cause: decoration paths were treated as render-only metadata instead of generated artifacts that need reference rewriting. Rule: whenever a layout declares `additionalRootDecorationPaths`, add those paths to generate-side source maps and test at least one nested Markdown mirror for canonical link cleanup.

- **Nested generated Markdown links need explicit current-directory prefixes**: `.cursor/AGENTS.md` was corrected from `.agentsmesh/skills/post-feature-qa/` to `skills/post-feature-qa/`, but the user expected the verified path from `.cursor/AGENTS.md` itself: `./skills/post-feature-qa/`. Root cause: `formatLinkPathForDestination()` stripped leading `./`, and the regression only asserted "not canonical" instead of the exact target-relative spelling. Rule: for generated Markdown inside a target folder, preserve/add `./` for same-directory descendant links and assert the exact rendered path in tests.

- **Global-mode e2e expectations must derive from target descriptors, not historical path helpers**: While consolidating link/path rebasing, the global path e2e suite failed on paths like Windsurf `workflows`, Gemini `workflows`, Roo `.roo-code`, and Codex `config.toml` even though production generation now uses target descriptor layouts. Root cause: the e2e expectations had drifted from descriptor constants and from the shared rewriter's protected URI behavior. Rule: when retiring path-rewrite helpers or changing global-mode tests, update e2e assertions from the target descriptor/constants and rerun them with the e2e Vitest config after `pnpm build`.

- **Global mode requires `rewriteGeneratedPath` in the target layout to transform project paths to global paths**: When implementing Copilot global mode, I initially only added the `global` layout with paths but forgot to add `rewriteGeneratedPath` to transform `.github/` paths to `.copilot/` paths. Root cause: I assumed the path resolvers in the layout would be sufficient, but the engine uses `rewriteGeneratedPath` to transform generator output paths before writing. Rule: when implementing global mode for a target, always add `rewriteGeneratedPath` to the global layout to transform project-level paths to global paths, and test that files are generated in the correct location.
- **Create new reusable skills in `.agentsmesh/skills`, not `.agents/skills`**: I initially added the global-mode target skill under `.agents/skills`, which bypassed the repo's canonical source-of-truth model for skills. Root cause: I followed the local runtime-visible skill tree instead of the project rule that `.agentsmesh/` owns editable canonical content and generated surfaces are artifacts. Rule: when adding or updating repo-owned skills in this codebase, create them under `.agentsmesh/skills/...` first and only rely on generation to project them into target-specific artifact folders.
- **Do not invent extra files inside canonical skill directories unless the canonical skill spec supports them**: I added `agents/openai.yaml` under a canonical skill even though the documented `.agentsmesh/skills` contract only supports `SKILL.md` plus supporting files. Root cause: I copied a pattern from existing repo content without checking it against the published canonical-config spec for skills. Rule: before adding a new file shape under `.agentsmesh/skills/<name>/`, verify it is part of the documented canonical format; otherwise keep the skill to `SKILL.md` and ordinary supporting files only.
- **Canonical loader refactors must preserve the project-root entrypoint contract**: I changed `loadCanonicalFiles()` to accept only a canonical-directory path, which immediately broke extend loading, install flows, and the loader test suite because much of the repo still passes project roots and expects the function to locate `.agentsmesh/` itself. Root cause: I optimized for the new global-scope path without preserving the existing public helper contract or sweeping every caller first. Rule: when making canonical path-loading scope-aware in this repo, keep loader helpers backward-compatible with both project-root and canonical-dir inputs until every caller is explicitly migrated and covered.

- **Watch unit timing budgets may need another bump after test-runner upgrades, even when isolated runs stay green**: After upgrading Vitest from `4.0.18` to `4.1.4`, the full `pnpm test` suite started flaking in `tests/unit/cli/commands/watch.test.ts` even though the same file still passed in isolation, because the heavier full-run scheduler pushed a `runMatrix` assertion past the existing 8000 ms wait budget. Root cause: I treated the previously hardened timeout as stable across runner upgrades without revalidating the full-suite latency envelope. Rule: when test-runner or watcher-adjacent dependencies change, rerun the full suite and increase watch-unit `vi.waitFor` budgets again if needed; isolated green runs are not enough.

- **Do not batch runtime packages into a `pnpm add -D` upgrade command**: While upgrading vulnerable dependencies, I included runtime packages (`diff`, `tar`) in the dev-dependency update command and silently moved them into `devDependencies`. Root cause: I optimized for fewer commands instead of preserving the runtime/dev boundary package by package. Rule: when upgrading mixed dependency types, run separate install commands for `dependencies` and `devDependencies`, then re-open `package.json` before considering the update pass correct.

- **Prefer a runner/toolchain that already satisfies npm trusted-publishing requirements over self-upgrading npm in CI**: The release workflow failed on `npm install -g npm@latest` on `ubuntu-latest` with `Cannot find module 'promise-retry'` inside npm's own arborist dependency tree before the publish step even ran. Root cause: I treated npm self-upgrade as the default fix for trusted publishing instead of checking npm's documented minimums and selecting a runner that already bundles a compatible npm. Rule: for npm trusted publishing, first choose a GitHub-hosted Node version whose bundled npm already satisfies the current docs (for example Node 24 in the current npm examples); only add an explicit npm install when a specific known-good version is required and the runner cannot supply it directly.

- **`execSync` shell must exist on Linux CI**: A lint integration test passed `shell: '/bin/zsh'` to merge stderr; GitHub Actions `ubuntu-latest` has no `/bin/zsh` → `spawnSync /bin/zsh ENOENT`. Root cause: macOS dev shells were assumed for all runners. Rule: for shell features like `2>&1` in integration tests, use `/bin/sh` (or omit `shell` and avoid shell-only syntax), never hardcode zsh-only paths unless the job installs zsh.
- **When test fixtures grow required manifest fields, update callback expectations in the same pass**: I added `source_kind` and `features` to `InstallManifestEntry` fixtures in `tests/unit/install/install-sync.test.ts`, which made standalone TypeScript clean, but the full Vitest run still failed because the `reinstall` assertions were still expecting the old `{ name, source }` payload. Root cause: I repaired the typed fixture shape without sweeping downstream expectations that compare the same object at runtime. Rule: whenever a test helper or fixture type gains required fields, update all `toHaveBeenCalledWith` / deep-equality assertions that observe that payload before calling the pass complete.
- **Repo typecheck that excludes tests must be supplemented when touching typed test fixtures**: A user still saw `TS2741` in `tests/unit/canonical/extend-load.test.ts` even though `pnpm typecheck` was green, because `tsconfig.json` excludes `**/*.test.ts` and the failing `ResolvedExtend` fixture only existed in a test file. Root cause: I relied on the repo typecheck gate without checking whether the touched file was inside the compiler include set. Rule: whenever a fix touches typed test files in this repo, run a standalone `tsc --noEmit ... <test-file>` (or an equivalent test-aware TS config) on the affected tests, especially for editor-reported TS errors.
- **If ESLint rules target tests, the lint script must target tests too**: Warning cleanup surfaced unused vars and return-type warnings in `tests/` even though `pnpm lint` was green, because the script only ran `eslint src/` while `eslint.config.js` defined rules for `tests/**/*.ts` as well. Root cause: lint command scope drifted from the configured file scope, so test warnings accumulated outside the default verification path. Rule: whenever ESLint config includes `tests/**` or other first-class source trees, keep the `lint` script aligned with those paths and ignore only generated artifacts explicitly.
- **Target-native filename corrections still need legacy import aliases when users may already have the old file**: After switching Cline to the corrected generated filename `cline_mcp_settings.json`, the importer stopped reading historical `.cline/mcp_settings.json` files and the Cline import e2e lost canonical MCP output. Root cause: I treated the filename correction as a full replacement across all read paths instead of separating generation/stale-cleanup contracts from backward-compatible import support. Rule: when a target-native filename changes, update generation and docs to the new path, but keep importer fallbacks for the previous on-disk filename unless the migration is explicitly destructive.
- **Watch tests need per-test temp roots under coverage load**: The full coverage run started flaking in `tests/unit/cli/commands/watch.test.ts` even though the watch logic was fine, because every case reused the same temp project path and chokidar state could bleed across teardown/recreate cycles. Root cause: the test fixture optimized for convenience by keeping one shared temp directory, which is unstable for file watchers when coverage slows timing and event cleanup. Rule: any watcher test in this repo must create a fresh temp project directory per test case and not reuse a prior watched path after teardown.
- **Path-preserving import refactors must update native install scope derivation and relative-path filters together**: After preserving nested canonical import paths, native install scope still derived command picks from `basename(...)` and the Cline rule mapper still looked for `"/workflows/"`, which broke Gemini namespaced command picks and caused Cline workflows to be double-counted as rules. Root cause: I updated canonical output paths without sweeping downstream code that still assumed flattened names or absolute-style path fragments. Rule: whenever imported canonical paths change shape, audit install-scope pick derivation and every relative-path classifier for basename/leading-slash assumptions, then rerun the native install scope suites.
- **Async importer serializer changes must update every `importFileDirectory` mapper to await them**: I switched Copilot prompt import to an async placeholder serializer but left the mapper callback synchronous, which wrote a `Promise` into `writeFileAtomic` and broke the importer suite. Root cause: I changed a shared helper contract without sweeping all mapper call sites that feed `importFileDirectory`. Rule: whenever an import path starts using an async serializer, make the mapper callback `async`, await the serializer result, and rerun the touched target importer suite plus `pnpm lint`/`pnpm typecheck` before broader verification.
- **New repo docs must not be authored under an ignored path without first narrowing the ignore rule**: I created the architecture documentation set and only afterward verified git visibility, which meant the files existed on disk but were still excluded by the root `/docs` ignore policy. Root cause: I treated the presence of existing tracked docs as evidence that new docs would also be trackable and skipped `git check-ignore` up front. Rule: before adding any new documentation tree in this repo, inspect `.gitignore` and run `git check-ignore -v` on a representative target path so the docs land in a trackable location or the ignore rules are narrowed intentionally.
- **Watcher startup must await chokidar readiness before doing the initial generate**: After reorganizing `install/` and `canonical/`, the full suite still showed flaky `watch` behavior even though the isolated watch suite passed, because `runWatch()` kicked off its startup generate before chokidar finished establishing watches. Root cause: startup lock churn and the first real edit could race watcher readiness under full-suite load, producing missed rule-add events and stray self-triggered regenerations. Rule: when a watch command performs an initial reconciliation/generate, wait for the watcher `ready` event first, then run the initial generate, and recheck both the isolated watch suite and full `pnpm test`.
- **Watch-mode ignore filters must account for parent directory events, not only specific generated files**: After splitting `src/core` into subfolders, the full suite still showed flaky `watch` behavior even though the targeted watch test passed in isolation, because lock writes could surface as `.agentsmesh` directory events under heavier suite load. Root cause: the ignore helper filtered `.agentsmesh/.lock` and `.agentsmesh/.lock.tmp` but not the parent directory metadata event emitted by the watcher. Rule: whenever watch mode suppresses self-generated file churn, ignore both the exact generated files and the canonical parent directory event, then validate with the isolated watch suite and the full `pnpm test` run.
- **Metadata consolidation needs immediate lint/typecheck gates before broader verification**: I centralized built-in target metadata and rewired registry/catalog lookups, but the first pass left stale skill-dir imports and widened map key types that only surfaced in `pnpm lint` and `pnpm typecheck`. Root cause: I changed the source-of-truth layer first and relied on targeted behavior tests before re-running static analysis on the touched plumbing. Rule: after any cross-cutting metadata refactor, run lint and typecheck immediately after the first green targeted tests, and type shared lookup maps (`Map<string, ...>` or narrow helper returns) so string-key lookups do not drift into widened unions.
- **Generated root decoration must avoid the `.agentsmesh/` substring when e2e asserts clean target prose**: The reference-rewrite matrix strips fenced code blocks then requires generated root files not to contain `.agentsmesh/`; the appended AgentsMesh contract used inline `` `.agentsmesh/` ``, which always failed that guard. Root cause: decoration text was written as a path literal without considering the strict “no canonical path token in target output” contract. Rule: when appending global instructions to generated targets, phrase canonical location without the exact `.agentsmesh/` substring (e.g. “the `.agentsmesh` directory at the project root”), keep legacy paragraph detection for upgrades, and strip both old and new contract blocks on import.
- **Centralized target metadata needs direct engine assertions, not only generator coverage**: I moved root-instruction decoration into engine metadata but initially sourced Cursor's primary path from a file with no exported constant, which only surfaced as a build warning and a stale unchanged-status expectation. Root cause: generator tests still passed because they no longer owned decoration, and there was no explicit engine assertion proving Cursor's registered primary root file was the one being decorated. Rule: whenever behavior moves from target generators into engine/registry metadata, add engine-level tests for each representative target path (especially compatibility targets like Cursor) and rerun build warnings as part of the refactor gate.
- **Canonical `.agentsmesh/` content must be edited before generated target artifacts**: I updated `.agents/skills/...` and `.github/skills/...` directly when fixing the release skill, and only afterward was reminded that `.agentsmesh/` is the source of truth. Root cause: I treated generated target files as primary because they were the visible diff, instead of tracing the change back to canonical source first. Rule: when a skill/rule/command/agent exists under `.agentsmesh/`, make the edit there first and regenerate affected targets rather than hand-editing `.agents/`, `.github/`, or other generated trees.
- **Trusted publishing must stay on the tested Changesets contract with a current npm CLI**: Release automation failed with `npm error code E404` during GitHub Actions trusted publishing while the repo still used a raw `npm publish` release script and the publish workflow never upgraded npm on the runner. Root cause: the release path drifted away from the repo's intended Changesets/OIDC flow and from current npm trusted-publishing guidance. Rule: keep `package.json` on `pnpm build && changeset publish`, set explicit public publish metadata (`publishConfig`, canonical `repository.url`), and upgrade npm in the publish workflow before the changesets publish step.
- **`pnpm link --global` needs `PNPM_HOME` in that shell**: It fails with `ERR_PNPM_NO_GLOBAL_BIN_DIR` when the process environment does not define `PNPM_HOME` (and global-bin-dir), which happens in some IDE-integrated terminals that skip full shell init even if `~/.zshrc` is correct for interactive use. Root cause: pnpm only knows where to place global shims when that variable is set. Rule: document or script a no-link dev path (`pnpm build` then `pnpm ab -- <args>` / `node dist/cli.js`); for global link, run `pnpm link --global` from a terminal where `echo $PNPM_HOME` is non-empty (e.g. `source ~/.zshrc` first).
- **Manual single-item installs should persist collection scope plus pick, not file scope alone**: I initially persisted manual single-file installs at the file path itself, which meant a later sibling install from the same source folder could neither match the existing pack nor merge the manifest subset cleanly. Root cause: the persistence identity for `install --as ... <single file>` was derived from the discovered file path instead of the parent collection plus selected item name. Rule: whenever a manual install targets one file or one skill from a broader collection, persist the parent collection path and explicit `pick` so later installs from the same collection can amend the same pack and installs entry.
- **Single-file remote installs need blob URL parsing, not just tree URL parsing**: I previously fixed sync replay and assumed single-file remote installs were covered, but GitHub `blob/...` URLs still fell through to local-path parsing and failed with `Path does not exist`. Root cause: the install parser only recognized `tree` repository URLs and pinned remote ids, not file-oriented `blob` URLs. Rule: whenever install supports single-file remote inputs, parser coverage must include both directory-style `tree` URLs and file-style `blob` URLs for each supported host.
- **Sync replays must restore the saved install scope, not just the saved source path**: I fixed `install --sync` to replay the caller’s validation flags, but it still reinstalled every item under the saved folder because the replay path ignored `installs.yaml` `pick`/`features` metadata. Root cause: sync reused normal discovery and rebuilt selection from the broad path instead of reapplying the persisted manifest scope. Rule: whenever install replay persists `features` or `pick`, sync must reapply that saved scope before validation, selection, and manifest rewrite so restored packs match the original install exactly.
- **Sync replays must preserve install validation semantics unless the user explicitly forces them**: I initially implemented `install --sync` by replaying every saved install with `force: true`, which silently bypassed the invalid-item prompts and conflict checks users get on a normal install. Root cause: I treated sync as a recovery shortcut instead of a batch replay of the same install contract. Rule: when replaying persisted installs, carry through the caller’s `--force` and related validation flags rather than hardcoding a more permissive mode.
- **Install sync must replay persisted remote source formats, not just user-entered URLs**: I added `.agentsmesh/installs.yaml` replay but initially forgot that successful remote installs persist pinned sources like `github:org/repo@sha`, not the original `https://.../tree/...` URL. Root cause: sync reused the normal install parser, while the parser only understood user-facing URL/SSH/local forms and treated pinned manifest sources as local paths. Rule: whenever a workflow persists internal source identifiers for replay, the replay parser must explicitly support those stored formats and be covered by a regression test that uses the persisted value verbatim.
- **Install needs an explicit manual collection mode in addition to native-path inference**: I initially treated installability as something that could always be inferred from the source path shape, which failed on repos that store compatible agent content in arbitrary folders like `agents/universal`. Root cause: install discovery was coupled too tightly to target-native directory heuristics instead of the user’s explicit intent. Rule: when a user specifies what kind of collection they want to install, provide an explicit override (`--as`) that bypasses path heuristics while keeping native auto-detection as the fallback.
- **Pack coverage must include settings features, not just array resources**: I initially focused pack install verification on skills/rules/commands/agents and missed that materialized packs were silently dropping canonical `mcp`, `permissions`, `hooks`, and `ignore` during write/load/merge. Root cause: the earlier tests only asserted array-resource behavior and dry-run previews, so the pack persistence contract for settings never ran through a real install. Rule: whenever install materializes canonical data, add tests that verify every supported canonical feature survives write → load → generate, including settings files and a real non-dry-run command path.
- **Native install discovery must be staged and importer-driven**: I initially let `install` write `.agentsmesh/` into the source repo during native discovery and still depended on native folder heuristics for subtree selection. Root cause: I reused the extend/import normalization path directly instead of treating install discovery as a read-only staging step. Rule: whenever `install` needs to understand a native agent structure, import into a temporary staged copy and derive folder/file scope from importer `fromPath` results rather than mutating or heuristically traversing the source checkout.
- **Incremental pack updates must match remote source identity, not the pinned ref**: Pack reinstall logic failed to find an existing remote pack once the upstream repo resolved to a new SHA because the lookup compared the full pinned `source` string (`github:org/repo@sha`). Root cause: install metadata stores the latest pinned provenance, but incremental-update detection needs a stable repo identity across versions. Rule: when materialized installs track pinned remote provenance, match existing packs by stable remote identity (provider + repo/url without ref) and refresh the stored `source`/`version` metadata after each successful merge.
- **Live generated targets must be checked after capability changes**: I claimed Continue/Junie reference rewriting was correct based on targeted tests, but the real generated repo still leaked `.agentsmesh/skills/...` links because those targets did not actually emit any skill artifacts. Root cause: I verified synthetic round trips without checking the live target outputs against the actual configured feature set. Rule: whenever a rewrite fix depends on target capability coverage, regenerate the real repo artifacts and grep the exact reported path pattern in the affected target files before declaring success.
- **Large-extend failures can come from generate-time caches, not import**: I initially treated the Codex remote-extend crash as an import/layout problem, but the cached repo had already imported into `.agentsmesh/skills`; the actual heap blowup came later when `rewriteGeneratedReferences()` rebuilt the full artifact map once per output file. Root cause: I inspected the import surface first and did not measure where the large canonical payload was consumed during generation. Rule: when a big extend triggers OOM, verify whether cached canonical import already succeeded, then audit per-output generation/rewrite caches before changing importer behavior.
- **JSDoc `/*` in block comments**: Using `/*` inside a JSDoc block (e.g. `.agentsmesh/skills/*/SKILL.md`) starts a nested block comment and breaks parsing. Reword to avoid (e.g. `skills/{name}/SKILL.md` or `glob: *.md`).
- **Branch coverage gaps**: To reach 85% branch coverage from ~79%, add tests for: remote-fetcher catch block (offline fallback path), engine.ts uncovered branches, merger/validate-rules, version.ts (both dist/src paths). Unit tests that call runGenerate/runWatch directly (same process) contribute to coverage; integration tests using execSync run in a subprocess and do not.
- **Nested fixture searches**: I incorrectly concluded `root-ab` had no skills because I used `find -maxdepth 3`, which hid `skills/{name}/SKILL.md` and nested reference files. Root cause: bounded search depth on nested fixture structures. Rule: when auditing fixture coverage, use a full-depth listing first or set depth high enough to include nested skill/reference files before drawing conclusions.
- **Canonical skills source**: I added `add-agent-target` only under `.agents/skills/`, which made it a target-specific artifact instead of canonical source data. Root cause: I optimized for local skill discovery and ignored the repo rule that `.agentsmesh/` is the source of truth. Rule: when adding or editing reusable repo skills, put the canonical copy in `.agentsmesh/skills/` first, then generate or align target-specific skill directories from there.
- **Parallel test isolation**: I ran the isolated watch test and the full Vitest suite in parallel even though they use the same fixed temp directory (`ab-watch-cmd-test`). Root cause: I parallelized verification without checking whether the tests shared filesystem state. Rule: never run test commands in parallel when they touch the same temp paths, ports, caches, or repo artifacts.
- **Fixture tree deletion**: I tried to remove the obsolete `root-ab` fixture with a shell `rm -rf`, and the environment blocked it as a destructive command. Root cause: I switched from the repo's patch-based editing workflow to a destructive shell command for tracked files. Rule: when deleting tracked fixture trees in this repo, remove them with `apply_patch` file deletions instead of shell `rm -rf`.
- **Targeted e2e runs use built output**: I ran new e2e slices immediately after patching source and misread failures because `tests/e2e/helpers/run-cli.ts` executes `dist/cli.js`, not source files. Root cause: I forgot that ad hoc `vitest` e2e runs do not build automatically the way `pnpm test:e2e` does. Rule: whenever e2e assertions exercise the CLI binary directly, run `pnpm build` before targeted e2e slices or use `pnpm test:e2e`.
- **Parallel e2e races on dist/**: I ran a targeted e2e slice in parallel with `pnpm test:e2e`, and the full run's `pnpm build` cleaned `dist/` mid-test, causing false CLI "module not found" failures. Root cause: I parallelized verification steps that share the same built output directory. Rule: never run two CLI e2e commands in parallel in this repo; serialize anything that reads `dist/cli.js` or rebuilds `dist/`.
- **CLI integration slices also require fresh dist/**: I ran targeted integration tests after changing CLI behavior and misread a missing generated prompt as an implementation bug, but the cause was stale `dist/cli.js`. Root cause: I remembered the e2e build rule and forgot that integration tests using `node dist/cli.js` have the same dependency. Rule: rebuild with `pnpm build` before any targeted CLI integration or e2e slice whenever source affecting `dist/cli.js` changed.
- **Watch must ignore generated lock artifacts**: Watch mode spammed `Nothing changed`/`Regenerated` in a loop because it observed `.agentsmesh/.lock`, and `generate` rewrites that file atomically on every pass (`.lock.tmp` then `.lock`). Root cause: the watcher subscribed to the whole `.agentsmesh/` tree without excluding its own bookkeeping files. Rule: whenever watch observes canonical config directories, explicitly ignore internal generated artifacts like `.agentsmesh/.lock` and adjacent temp files so self-generated writes cannot retrigger the pipeline.
- **Support-level changes require a full target audit**: I updated the compatibility matrix for embedded agent support but initially left target-specific lint messaging behind, which would have continued warning that the same features were unsupported. Root cause: I treated matrix/docs as the whole support contract and did not audit target diagnostics alongside generator/importer behavior. Rule: whenever a target's support level changes, update and verify the matrix, docs, generator, importer, and target-specific linter messaging together.
- **Remote cache compatibility matters**: I broadened remote extend support and accidentally changed the GitHub cache-key layout, which broke reuse of already-cached GitHub extends and caused refresh tests to look in the wrong directory. Root cause: I generalized the cache naming scheme without preserving the existing GitHub on-disk contract. Rule: when refactoring cache-backed features, preserve existing cache paths for supported providers unless an explicit migration is added and verified.
- **Node globals are not implicit in this lint setup**: I used `URL` directly in new config helpers and lint failed with `no-undef`. Root cause: I assumed Node's runtime global would satisfy the repo's ESLint environment. Rule: when adding Node platform globals in source files here, import them explicitly from the relevant `node:` module instead of relying on ambient globals.
- **Union type refactors need serializer narrowing**: I widened `McpServer` to support URL-based transports, but left the Codex TOML serializer accepting the broad union and only caught it when `pnpm typecheck` failed on `server.command`/`server.args`. Root cause: I updated the source model without re-narrowing downstream sink functions that require a specific variant. Rule: whenever a core type becomes a union, add explicit type guards at every serializer/generator boundary and rerun `pnpm typecheck` before claiming the refactor is done.
- **Reference rewrite audits must check real generated prose, not only exact file refs**: I declared `.agentsmesh/...` reference rewriting fixed, but the generated root instructions still contained skill directory links like `.agentsmesh/skills/post-feature-qa/`. Root cause: my tests covered exact file paths (`SKILL.md`, supporting files) and missed directory-style references used in the actual canonical root rule text. Rule: whenever path rewriting is added or changed, search real canonical content for path patterns and add tests for both exact files and directory-form references before calling the feature complete.
- **Shared artifact collision checks must compare canonicalized rewritten content**: I treated Codex/Windsurf `AGENTS.md` overlaps as ordinary file collisions after adding target-specific reference rewriting, which broke generation even when both outputs came from the same canonical root rule and only differed by rewritten target paths. Root cause: collision handling compared post-rewrite bytes without considering that some overlapping compatibility artifacts intentionally project the same canonical content through different target-specific links. Rule: whenever multiple targets share one output path, add coverage for rewritten-content overlaps and compare the outputs after normalizing target-specific references back to canonical paths before deciding they truly conflict.
- **Fixture-driven e2e requests should use the richest existing fixtures first**: I initially added import-reference e2e coverage with a synthetic inline project even though the user asked for the richest structure fixtures. Root cause: I optimized for speed and forgot that realistic fixture layouts are part of the requirement when verifying path-heavy behavior. Rule: when a user asks for e2e coverage over realistic structures, start from the richest existing fixture and only mutate the copied temp fixture as needed instead of inventing a smaller synthetic project.
- **Import-side reference normalization must not rewrite bare root filenames in prose**: I normalized `AGENTS.md` and similar root artifact names as raw substrings, which changed ordinary prose like `# Windsurf via AGENTS.md` into canonical paths. Root cause: the reverse rewriter treated root filenames as stable path tokens even though they are common standalone words in instructions. Rule: in import-side reference maps, only normalize target paths that uniquely identify canonical artifacts; do not rewrite bare root filenames like `AGENTS.md`, `CLAUDE.md`, `.windsurfrules`, or `GEMINI.md` unless they appear in a more structured path form.
- **Refactor follow-through includes test helper APIs**: I replaced the string-based import reference helper with a file-aware normalizer, but left one unit test file calling the removed function and only caught it during the full `pnpm test` run. Root cause: I updated behavior-facing tests first and forgot to audit direct unit tests that imported the old helper by name. Rule: whenever a core helper API is replaced or removed, run a targeted search for all direct imports/usages and update those tests before relying on the broader suite.
- **Cleanup passes still need typecheck**: I removed projected-agent rebasing bugs, but left unused destructuring behind in two importers and only caught it at the end of `pnpm typecheck`. Root cause: I treated a small cleanup as harmless after tests passed and deferred type-level verification too late. Rule: after any refactor that changes parsing/normalization control flow, rerun `pnpm typecheck` immediately after the code edits, even if the logic change seems minor.
- **Generated-source maps must include every emitted artifact path, not just primary target files**: I mapped Windsurf root rules only to `.windsurfrules`, which left the additional shared `AGENTS.md` outputs outside the generate-side rebasing pass and caused false Codex/Windsurf collisions after import-normalized relative links. Root cause: I treated the reference map as equivalent to the generator's full output set and forgot that one canonical rule can emit multiple artifacts. Rule: whenever a target generates more than one file from the same canonical source, audit the output-source map against the full generator output list so every emitted path participates in rewriting and collision logic.
- **Contract changes must audit “unchanged by design” cases separately from successful rewrites**: I updated the link contract to use project-root paths and initially changed several expectations for references that are intentionally left untouched when the target file does not exist, such as missing checklist refs and the fixture’s invalid `../SKILL.md`. Root cause: I treated all relative-looking references as resolvable without re-checking the existence-based guard in the rewriter. Rule: whenever the link normalization contract changes, explicitly audit both categories: links that should rewrite and links that should stay unchanged because resolution fails.
- **Root-relative link contracts need root-first resolution for bare multi-segment paths**: After switching generated links to project-root-relative paths without a leading slash, I still resolved tokens like `src/AGENTS.md` relative to nested source files, which broke Codex skill imports and only showed up in the rich e2e fixture. Root cause: the resolver only treated known dot-prefixed prefixes as root-relative and assumed other multi-segment paths were source-relative. Rule: when the contract says generated internal links are project-root-relative, resolve bare multi-segment paths from the project root first, then fall back only when the root candidate is not valid, and add nested-file tests for cases like `src/AGENTS.md` and `docs/...`.
- **Multi-output targets need per-output rewrite maps, not just per-target maps**: Copilot emits both `.github/copilot/*.instructions.md` and `.github/instructions/*.md` from the same canonical rule, and I initially cached the rewrite map only by target, which left the new output family using the wrong translation rules and skipping path normalization. Root cause: I assumed one canonical-to-target artifact map was sufficient for every file a target emits. Rule: whenever a target can generate multiple artifact families from the same canonical source, build and cache rewrite maps per output path or per artifact family, and add tests for each emitted path family.
- **Import normalization must map realpath variants of generated artifacts**: I expanded the path rewrite matrix and then left import normalization keying only on joined target paths, which failed in temp-workspace runs where referenced files resolved through a realpath variant and stayed target-specific after import. Root cause: the import artifact map did not canonicalize both direct and realpath forms of generated file paths before translation. Rule: whenever path translation depends on filesystem lookups, include both normalized and realpath variants in the lookup map and add tests that exercise absolute in-project paths through the importer.
- **Path tokenizers must exclude standalone separators used as prose markers**: I treated the leading `/` branch in the shared link regex as “any slash,” which caused status text like `✓ / ✗` to enter the rewrite pipeline and be reported as a missing path. Root cause: the absolute-path branch did not require any actual path segment after `/`. Rule: when broadening path detection, require enough structure to distinguish real file references from prose separators, and add regression tests for marker text like `✓ / ✗`.
- **Cross-platform path fixtures must use valid paths for each file location**: I added Windows-style e2e coverage and briefly used `..\\SKILL.md` from inside `skills/api-generator/template.ts`, which is not a valid sibling reference and caused a false failure that looked like a resolver bug. Root cause: I translated a POSIX relative-path pattern into Windows separators without re-checking the file’s actual directory structure. Rule: when adding cross-platform path tests, validate each fixture path against the real file tree first and only assert normalization on paths that genuinely resolve.
- **URI-skip patterns must not swallow Windows drive letters**: I added protected ranges for external refs like `ssh://...` and `git@github.com:...`, but the first scheme regex also matched `C:` and blocked legitimate Windows absolute paths from being rewritten. Root cause: I treated any single-letter scheme prefix as a URI without separating it from Windows drive syntax. Rule: when protecting URI-like references in this codebase, keep the scheme matcher distinct from drive-letter path detection and add a regression test that covers both in the same file.
- **Real canonical content can contain over-traversal relative refs that still mean repo-root files**: I declared the path rewrite matrix complete, but a checked-in skill still used `../../../../docs/agents-folder-structure-research.md` and generation/import left it unchanged. Root cause: the resolver only tried source-relative normalization for `../` paths and never retried from the repo root when the normalized path escaped above the workspace. Rule: whenever relative-link handling changes, include over-traversal cases from real canonical files in both unit and all-target generate/import matrices, and add a project-root fallback candidate for `../` refs that escape the repo but match an in-repo path.
- **Rewrite-contract changes need a second pass over edge-case suites**: I changed inline-code path rewriting and updated the main unit/e2e coverage first, but left `link-rebaser-edge-cases.test.ts` asserting the old protected-inline behavior and only caught it during the repo-wide `pnpm test` run. Root cause: I audited the primary behavior tests and forgot the separate edge-case suite that codifies the same contract. Rule: whenever the link-rewriter protection contract changes, search both the main rewrite tests and `link-rebaser-edge-cases` for matching expectations before running the full suite.
- **Format-contract work needs the research and round-trip suites, not only e2e slices**: I aligned the generators/importers and got the focused e2e suite green, but `pnpm test` still failed because `tests/agents-folder-structure-research.test.ts` and `tests/import-generate-roundtrip.test.ts` were asserting the legacy file layouts. Root cause: I treated the main e2e matrix as the full contract and forgot the separate research/round-trip suites that also codify official target formats. Rule: whenever a target file-structure contract changes, update and run the research, round-trip, unit, integration, and e2e suites before declaring verification complete.
- **Generated-structure tests must assert exact artifact sets**: Copilot hook coverage passed while still missing the guarantee that every JSON-referenced wrapper script exists, because the tests only checked that some hook files existed. Root cause: the suite used loose structural assertions (`some`, prefix matches, "at least one") instead of exact path lists and exact counts. Rule: whenever a test verifies generated files or directories, assert the exact relative path set and exact count, and cross-check referenced artifacts like hook wrapper scripts against the config that points to them.
- **Parallel generators for one feature must share the same entry filter**: Copilot hook JSON generation treated empty-string commands as valid because it only checked `typeof command === "string"`, while wrapper generation skipped falsy commands and silently produced fewer files. Root cause: two outputs for the same canonical hook set implemented their own “is this hook runnable?” logic. Rule: whenever one canonical feature emits multiple artifact families, centralize the eligibility predicate and add a regression test that compares the referenced artifact list against the emitted files.
- **Target research must separate similarly named products and runtimes**: I answered questions about Codex MCP by mixing the generated `codex-cli` target contract with the current Codex desktop chat runtime, which uses a different configuration surface. Root cause: I treated the target name as equivalent to the active runtime instead of verifying the exact product surface. Rule: when researching or documenting a target, first identify whether the task is about CLI, desktop app, chat product, or repo artifact generation, and verify MCP/config paths for that exact surface before making claims.
- **Exact e2e tree assertions must account for generated lock artifacts**: I added a strict Junie generate e2e assertion and forgot that `generate` writes `.agentsmesh/.lock`, so the otherwise-correct target output failed the exact tree check. Root cause: I copied the canonical tree expectation without auditing the post-generate bookkeeping artifacts that the CLI always emits. Rule: whenever an e2e test asserts the exact generated repo tree, include `.agentsmesh/.lock` if the command under test runs `generate`.
- **Dropped target metadata needs an explicit lint warning**: Codex CLI was silently discarding MCP `description` fields during generation because the target serializer does not project them into `.codex/config.toml`, and I initially explained the behavior without surfacing it through diagnostics. Root cause: I treated unsupported metadata as an implementation detail instead of part of the target contract users need to see during linting. Rule: whenever a target generator intentionally drops canonical metadata, add or update a target-specific lint warning and cover it with unit plus CLI e2e tests.
- **Re-import tests must cover existing canonical metadata, not only empty-project round trips**: Cursor command re-import looked fine in empty-project import tests, but it still stripped `description` and `allowed-tools` when importing generated body-only target files back into an existing `.agentsmesh` tree. Root cause: I tested import completeness and empty-project round trips, but not the real user flow where import overwrites existing canonical files. Rule: whenever a target format cannot represent all canonical metadata, add strict tests that re-import into an existing canonical project and verify unsupported fields are preserved from the canonical destination instead of being erased.
- **Importers must clean stale reserved artifacts when the canonical mapping changes**: After fixing the Codex importer to stop creating hidden/fixture scoped rules and `_ab-*` canonical skill dirs, a live re-import still showed the bad folders/files because the importer only added new outputs and never removed the stale ones from earlier buggy runs. Root cause: I treated import as write-only and forgot that reserved generated names like `_ab-agent-*`, `_ab-command-*`, and skipped scoped-rule names need cleanup on re-import. Rule: when an importer maps reserved/generated source artifacts back into canonical files, add strict re-import tests that preseed stale outputs and verify the importer removes them.
- **Shared importer rules belong in shared code, not copy-pasted per target**: Windsurf had the same hidden/fixture scoped-`AGENTS.md` and stale `_ab-agent-*` cleanup bug right after Codex because both targets implemented parallel importer logic separately. Root cause: I fixed one importer locally instead of extracting the shared contract. Rule: when two targets implement the same import pattern, move the filter/cleanup logic into a shared helper and verify both targets against the same strict regression cases.
- **Import normalization must recover from polluted generated artifacts across target boundaries**: I fixed Codex/Windsurf root imports but still left the repo vulnerable when a different target like `cline` or `claude-code` re-imported a generated root file that already contained Codex-style `.agents/...` links. Root cause: import normalization only loaded rewrite maps for the source target, assuming generated artifacts were pristine and same-family. Rule: when importing any target artifact, build the normalization map from every supported generated path family so re-import can always recover canonical `.agentsmesh/...` links from previously polluted outputs.
- **Coverage matrices must include root-rule references inside non-root files for every target**: I already had broad import/generate rewrite tests, but they still missed Copilot leaving `.github/copilot-instructions.md` inside canonical skill prose because the matrix did not combine exact file-structure assertions with richer cross-file root-rule references for every target on one round trip. Root cause: coverage was split across several suites, each strong locally but not strict enough about the full target contract. Rule: when validating target contracts, add a per-target e2e matrix that checks exact generated files, exact imported canonical files, and representative root/agent/skill link canonicalization on the same generate→import flow.
- **Empty hook commands must be rejected by shared logic, not per-target guesses**: A `Notification` hook with `command: ""` kept reappearing because canonical parsing and Gemini import/generate only checked `typeof command === "string"`, while other paths skipped falsy commands. Root cause: hook eligibility was duplicated across parser, importer, and generator code with different definitions of “valid command”. Rule: centralize trimmed hook command extraction in one helper and use it everywhere hooks are parsed, imported, linted, or generated, with strict tests for empty-string and whitespace-only commands.
- **Canonical booleans should be path-derived and explicit**: Non-root canonical rules drifted between `root: false` and no `root` field at all because each importer serialized rule frontmatter differently. Root cause: the canonical rule contract depended on per-target importer habits instead of one shared serializer. Rule: whenever a canonical field is derivable from the canonical destination path or artifact kind, derive and serialize it in one shared helper and add cross-target tests that assert the exact canonical frontmatter contract.
- **Target feature expansion must update both rewrite path maps and import scanners**: I added Junie commands/agents generation but initially missed reference-map path projection and non-root rule import scanning, which left generated links un-rewritten and round-trip imports incomplete. Root cause: I implemented generator/importer surface files first and forgot the shared path-translation contracts used by rewrite and import normalization. Rule: whenever enabling a new artifact family for a target, update `reference-map-targets` + import-reference map builders + importer scanners together, then verify with target-contract matrix e2e.
- **Shared install-persistence changes need the full entity matrix rerun**: I normalized manual single-item installs to persist their parent collection path plus explicit picks, but initially only updated the command regression and left agent/rule/skill matrix tests asserting the old file-path behavior. Root cause: I verified the user-reported entity in isolation instead of rerunning the broader manual install suites that share the same persistence contract. Rule: whenever install manifest or pack persistence semantics change, rerun and update the full agents/commands/rules/skills matrix, including sibling-item amend cases.
- **Pack naming must describe scope, not whichever item was installed first**: Manual single-item installs were naming packs from the first picked entity, which made sibling-item amendments keep misleading folder names like `repo-code-standards-enforcer` even after the pack grew into a broader collection. Root cause: auto-generated install names reused first-pick naming that works for individual extends entries but not for materialized pack containers. Rule: generate pack names from stable source identity plus normalized install scope, and cover root, scoped, and sibling-amend flows in tests.
- **Stable repo-feature naming requires multi-path provenance, not just a new string format**: I first moved pack names away from first-picked-item naming, but the user wanted all same-repo/same-feature installs to append into one pack, which breaks replay if manifest metadata still stores only one path. Root cause: pack naming and install identity were coupled to a single-path persistence model. Rule: when pack identity is broader than one path, persist aggregated `paths`, match packs by repo identity + feature set, and test sync replay across multiple folders of the same repo/feature.
- **Feature-specific linting must not be nested under unrelated feature gates**: I found that `runLint()` skipped command, MCP, permission, and hook diagnostics whenever `rules` was disabled because the whole per-target loop was gated on `hasRules`. Root cause: lint orchestration mixed per-target dispatch with a single feature guard instead of evaluating each lint family independently. Rule: when a command validates multiple canonical features, gate each lint family by its own feature flag and add a regression test for configs that enable non-rule features without `rules`.
- **Legacy sync replay needs compatibility for umbrella skill folders with nested child skills**: A persisted manual skill install could replay `path: skills/engineering` plus `pick.skills: [release-manager]`, but current staging treated `skills/engineering` as a single skill because it contains its own `SKILL.md`, so sync failed with `No resources match...`. Root cause: manual skill discovery only modeled “single skill” versus “skill collection” from the current folder shape and ignored legacy replay picks that target descendant skills. Rule: when replaying manual skill installs, let saved picked skill names disambiguate umbrella skill containers with nested child skills, and cover the legacy manifest shape with both staging and sync regression tests.
- **Strict install assertions must follow the persisted manifest contract, not assumptions from config or source paths**: While tightening install tests, I initially assumed manual installs would always persist fields like `target`, normalized `path`, and alphabetized picks, which caused new regressions in the hardening pass. Root cause: I wrote stricter expectations from inferred intent instead of checking the actual serialized manifest contract for each install mode. Rule: when converting loose manifest assertions into exact ones, inspect the real persisted entry shape first and lock tests to that observed contract, including field omission and ordering behavior.
- **Replay-scoped installs must carry the narrowed canonical all the way into pack materialization**: I fixed sync replay selection for saved `features`/`pick`, but pack reinstall still wrote unrelated settings like `mcp` and `ignore` because `runInstall()` passed the pre-replay canonical into `installAsPack()`. Root cause: replay narrowing was only applied to pool selection and diagnostics, not to the canonical payload persisted into the pack. Rule: whenever replay or scoped install narrows canonical features, pass that narrowed canonical through every downstream write/materialization path and add a regression test that proves excluded settings are absent from the resulting pack.
- **Manual skill replay must recover picked descendants from collection roots, not only umbrella skill folders**: I fixed nested replay for `path: skills/engineering` but missed the live manifest case `path: skills` with `pick.skills: [release-manager]`, where the selected skill lives under `skills/engineering/release-manager`. Root cause: the replay staging helper only special-cased roots that themselves had `SKILL.md`, and collection-root staging still copied top-level directories instead of exact picked descendants. Rule: when replaying manual skill installs with preferred picks, search all descendant `SKILL.md` paths under the source root and stage the exact matched skill directories whenever the requested names resolve uniquely.
- **Do not trust `install --sync --dry-run` to be side-effect free without proving it**: I used the built CLI with `install --sync --dry-run --force` to validate the live manifest and it still rewrote pack state in the workspace, forcing a manual restoration of those files. Root cause: I assumed dry-run semantics were enforced consistently on the sync replay path without first checking the actual CLI behavior. Rule: when validating sync replay in this repo, use isolated temp projects or inspect the implementation first; only run the live workspace command after confirming dry-run really short-circuits writes.
- **Manual install persistence and staging must agree on skill naming**: Manual skill installs persisted `pick.skills: [basename(contentRoot)]` while staging used `readSkillFrontmatterName()`, so the persisted pick never matched the staged skill when the SKILL.md had a frontmatter `name` field. Root cause: two independent code paths derived the skill name from different sources without sharing a helper. Rule: whenever install staging derives a skill name from frontmatter, the persistence layer must use the same derivation so pick values match the materialized pack tree.
- **Pack skill artifact paths must appear in the rewrite map alongside canonical paths**: Generated skill files referenced `.agentsmesh/packs/.../skills/.../reference/...` instead of target-relative paths because `buildArtifactPathMap` only mapped canonical `.agentsmesh/skills/...` paths. Pack skills have `source` pointing into `.agentsmesh/packs/`, and the link rebaser resolves relative refs against the actual source path. Root cause: the artifact map assumed all skill paths would be canonical-form, ignoring pack-form absolute paths. Rule: whenever skills can originate from packs, add pack-path→target-path entries to the artifact map so the reference rewriter can translate both canonical and pack absolute paths.
- **Generated root-instruction decorations must be stripped on import back to canonical**: After adding Codex rule indexes and rerunning the target-contract matrix, imported `_root.md` files for several targets still contained the generated AgentsMesh paragraph and failed canonical link-cleanliness assertions. Root cause: generation decorates primary root instruction files, but shared import serialization wrote those decorations back into `.agentsmesh/rules/_root.md` unchanged. Rule: whenever root instruction outputs gain generated sections or decoration, strip them in the shared root-rule import path before serializing canonical `_root.md`, and cover it with round-trip e2e tests.
- **Auxiliary rule folders must not duplicate a target's primary root file unless the spec explicitly asks for it**: I initially mirrored Codex `_root.md` into `.codex/instructions/` even though Codex already uses `AGENTS.md` as the main instruction file and the user only wanted additional rules there. Root cause: I interpreted "all canonical rules" literally instead of preserving the target's established root surface and the user's scope correction. Rule: when adding a secondary rule projection folder for a target with an existing primary root file, default to mirroring only non-root/additional rules unless the spec explicitly requires a root duplicate.
- **Dist-backed e2e and integration runs require a rebuild before trusting failures**: I fixed the Gemini importer in `src/` and kept seeing the old failure because the e2e harness executes `dist/cli.js`, not the source tree directly. Root cause: I reran targeted dist-backed tests after source edits without rebuilding, so I was debugging stale output instead of the current code. Rule: after any CLI behavior change, run `pnpm build` before interpreting e2e or integration results that invoke `dist/cli.js`.
- **User corrections about target-native filenames must override stale repo assumptions everywhere**: The repo still used `.cline/mcp_settings.json` across code, tests, and docs after the user clarified that Cline should use `cline_mcp_settings.json`. Root cause: target-native filename assumptions had drifted and were repeated in multiple layers without one explicit correction pass. Rule: when a user corrects a target-native path or filename, update the constant first and then sweep generators, importers, stale-cleanup, tests, and docs in the same change so the contract cannot stay split-brained.
- **Watch `vi.waitFor` thresholds must survive `pnpm test:coverage`**: `runMatrix` and `Regenerated.` assertions in `watch.test.ts` passed under plain `pnpm test` but timed out at 3000 ms when the same suite ran with coverage instrumentation. Root cause: coverage slows the process enough that chokidar debounce plus generate occasionally exceeded the minimum wait budget. Rule: keep watch unit `vi.waitFor` timeouts comfortably above 3000 ms (for example 8000 ms) and idle-stability delays at least 3000 ms when those tests must gate release prep and CI coverage.
- **Resolve merge markers before trusting any verification result**: `pnpm build` failed immediately on `src/core/generate/output-overlap.ts` because unresolved `<<<<<<<` conflict markers were left in the source file. Root cause: an incomplete merge left the workspace in a syntactically invalid state, so higher-level debugging would have been noise. Rule: when a build or test fails with parser errors, scan the touched files for merge markers first and clear them before deeper investigation.
- **Descriptor `rewriteGeneratedPath` must import every constant it references**: Cursor global generation failed at runtime with `ReferenceError: CURSOR_COMPAT_AGENTS is not defined` because the global layout branch referenced `CURSOR_COMPAT_AGENTS` without adding it to `index.ts` imports. Root cause: the global layout lived alongside project constants but the import list was not re-audited when the branch was added. Rule: after any target `rewriteGeneratedPath` or layout edit, run `pnpm exec vitest run` for that target’s `generate --global` CLI test (or add one) and `pnpm typecheck`; treat missing symbol imports as a release gate.
- **Global import must not treat `homedir()` like a repo root for recursive scans**: `agentsmesh import --from codex-cli --global` passed `homedir()` as `projectRoot`, and nested `AGENTS.md` import used `readDirRecursive(homedir())`, blowing the stack on symlink cycles under `~/.cache` and scanning the entire home tree. Root cause: project-only nested discovery was wired without a `scope === 'global'` guard. Rule: when `scope` is `global`, only read tool-native subtrees under the home root (e.g. `~/.codex`, `~/.agents/skills`); never run whole-tree `importFileDirectory` / `readDirRecursive` from `homedir()` for nested package rules.

- **Global mode implementation requires TargetCapabilities import**: When implementing global mode for roo-code, I initially forgot to import `TargetCapabilities` type from `target.interface.js`, which caused a TypeScript compilation error. Root cause: I copied the pattern from Junie but didn't verify all required imports were present. Rule: when implementing global mode for a target, ensure all required types are imported (`TargetCapabilities`, `TargetLayout`) and run `pnpm typecheck` immediately after adding the global layout configuration.

- **Global mode implementation follows a consistent pattern across targets**: When implementing global mode for Gemini CLI, I followed the established pattern from Copilot and other targets: (1) add global path constants, (2) create a `global` layout with `rewriteGeneratedPath` to transform project paths to global paths, (3) add `mirrorGlobalPath` for cross-agent skill mirroring, (4) define `globalCapabilities`, (5) add `globalDetectionPaths`, and (6) update help text and documentation. Root cause: the codebase has a well-established pattern for global mode that works consistently across all targets. Rule: when adding global mode to a new target, follow the existing pattern from targets like Copilot, Cursor, or Roo Code, ensuring all components (constants, layout, capabilities, detection, docs) are updated together, and verify with the full test suite.

- **Global mode paths must be namespaced to avoid conflicts between targets**: When implementing Gemini CLI global mode, it initially conflicted with Antigravity because both tried to write to `~/.gemini/GEMINI.md`. Root cause: Antigravity (a Gemini-based tool) was using the top-level `.gemini/GEMINI.md` path instead of a namespaced path like `.gemini/antigravity/GEMINI.md`. Rule: when multiple targets share a common parent directory in global mode (like `.gemini/`), each target must use a namespaced subdirectory to avoid path collisions. The collision detection in `src/core/generate/collision.ts` only handles `AGENTS.md` files specially, so other conflicts will throw errors. Always namespace global paths: Antigravity → `.gemini/antigravity/`, Gemini CLI → `.gemini/` (root level since it's the native tool).

- **Structure validation tests must match actual target generator APIs**: When creating structure validation tests for all targets, I initially assumed all targets exported the same set of generator functions (generateCommands, generateAgents, generateSettings, generateIgnore, etc.). However, each target has its own API surface - some use `generateWorkflows` instead of `generateCommands`, some combine settings into other functions, and some use different file formats (TOML vs YAML vs JSON). Root cause: I created tests based on a generic pattern without first auditing each target's actual exports. Rule: before writing tests for multiple similar modules, first audit the actual API surface of each module and create tests that match their real interfaces, not assumed interfaces.

- **Global mode structure validation tests must use the engine's `generate()` function, not `runGenerate()`**: When creating structure validation tests for global mode, I initially tried to use `runGenerate()` from the CLI commands, but that returns an exit code (Promise<number>), not the generated files. Root cause: I assumed the CLI command would work the same way as the generator functions used in project-mode tests. Rule: for global mode structure validation tests, use `generate()` from `src/core/generate/engine.ts` with `scope: 'global'`, and create a `makeConfig()` helper that returns a ValidatedConfig object with the target and features configured.
- **Test helper functions like `findGeneratedFile` expect specific parameter formats**: The `findGeneratedFile` helper accepts either a plain string for exact path matching, or an object with `stringContaining` or `stringMatching` properties. I initially used `{ exactPath: '...' }` which doesn't exist. Root cause: I didn't check the actual function signature before using it. Rule: when using test helper functions, check their actual parameter types first - for `findGeneratedFile`, use a plain string for exact matches, not `{ exactPath: string }`.
