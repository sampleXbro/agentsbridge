# Route Checklist

- Confirm the request and response schema before writing handlers.
- Reuse the shared validation helpers instead of hand-rolling parsing.
- Add request, success, and failure test coverage for the generated endpoint.
