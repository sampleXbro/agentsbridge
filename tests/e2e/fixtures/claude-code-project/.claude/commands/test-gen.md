---
description: Generate tests for a file
allowed-tools: Read, Write, Bash(npm run test)
---

Generate unit tests for the file at $ARGUMENTS.
Use Vitest. Follow nearby test patterns, cover the happy path plus one failure path,
and keep setup fixtures reusable.
