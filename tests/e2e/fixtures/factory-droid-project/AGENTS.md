# Project Instructions

Follow the project coding standards. Keep changes focused and well-tested.
Use TypeScript strict mode and prefer explicit return types.

When working on this codebase, always run the full test suite before
submitting changes. Prefer small, incremental commits over large refactors.
