# Project Rules

- Use pnpm as the package manager.
- TypeScript strict mode is required.
- Document non-obvious functions and public workflows with concise JSDoc.
- Run targeted tests before broader validation commands.
