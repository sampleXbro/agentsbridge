# Kilo Code project fixture

Realistic Kilo Code config tree used by agentsmesh e2e import + generate
suites. Mirrors a typical Kilo project: an `AGENTS.md` root, scoped rules,
slash commands, first-class subagents, a skill bundle, an MCP server config,
and a `.kilocodeignore` file.
