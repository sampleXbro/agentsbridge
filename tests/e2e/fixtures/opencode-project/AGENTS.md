# Project Rules

Use Test-Driven Development: write a failing test before implementing any feature.

Keep changes minimal and focused; no opportunistic refactoring alongside feature work.

Use `pnpm` as the package manager. Run targeted Vitest slice after each change.

Read neighbouring modules before modifying shared code to understand existing contracts.
