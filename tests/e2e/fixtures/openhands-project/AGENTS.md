# Project Instructions

This service exposes the billing API. Keep changes small and reviewable.

- Run `pnpm test` before every commit; never skip a failing test.
- Use TypeScript strict mode and explicit return types on exported functions.
- Database migrations live in `migrations/` and are append-only.
