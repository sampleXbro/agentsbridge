---
description: Create a conventional commit for staged changes
---

Create a git commit for the currently staged changes using the conventional commit format.

Format: `type(scope): message`

Types: `feat`, `fix`, `refactor`, `test`, `docs`, `chore`

Keep the subject line under 72 characters. Add a body if the change needs explanation.
