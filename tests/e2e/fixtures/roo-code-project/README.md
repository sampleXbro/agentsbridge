# Roo Code Project Fixture

Example project with Roo Code configuration for agentsmesh e2e tests.

## Structure

- `.roo/rules/` — general rules applied to all modes
- `.roo/commands/` — slash commands
- `.roo/skills/` — on-demand skills
- `.roo/mcp.json` — MCP server configuration
- `.rooignore` — file access restrictions
